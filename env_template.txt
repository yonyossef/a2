# Notification Settings
# Enable/disable notification methods (True/False)
SMS_NOTIFY_ENABLE=False
EMAIL_NOTIFY_ENABLE=True

# Twilio Configuration (Required if SMS_NOTIFY_ENABLE=True)
# Get these from https://console.twilio.com/
TWILIO_ACCOUNT_SID=your_account_sid_here
TWILIO_AUTH_TOKEN=your_auth_token_here
TWILIO_PHONE_NUMBER=+1234567890
YOUR_PHONE_NUMBER=+1234567890

# Email Configuration (Required if EMAIL_NOTIFY_ENABLE=True)
# Option 1: SendGrid API (Recommended for cloud platforms like Railway)
# Get API key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=your_sendgrid_api_key_here
EMAIL_FROM=noreply@example.com
EMAIL_TO=recipient@example.com

# Option 2: SMTP (For local use or platforms that allow SMTP)
# For Gmail: Use an App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
EMAIL_USERNAME=your_email@gmail.com
EMAIL_PASSWORD=your_app_password_here
# EMAIL_TO is set above for SendGrid, or set here if using SMTP only

